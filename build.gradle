plugins {
    id 'maven-publish'
    id 'signing'
    id "io.github.gradle-nexus.publish-plugin" version "1.0.0"
}

allprojects {
    apply plugin: 'java'
    apply from: "$rootDir/gradle/versioning.gradle"
 
    repositories {
        mavenCentral()
    }
}